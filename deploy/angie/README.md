Angie proxy configuration placeholder.

If you intended "Angie" as a different reverse proxy, place the appropriate config here.

Example: create `deploy/angie/angie.conf` and a small Dockerfile that copies it into the proxy image.
